const path = require("path");

const INTRO_STYLES = {
  title: "Introducing styles",
  description: "",
  filepath: path.resolve(__dirname, "intro-to-styles.md"),
  dependencies: [],
};

module.exports = {
  INTRO_STYLES,
};
